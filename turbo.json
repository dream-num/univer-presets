{
    "$schema": "https://turbo.build/schema.json",
    "extends": [
        "//"
    ],
    "tasks": {
        "build:preset": {
            "cache": false
        },
        "lint:types": {
            "cache": false
        }
    }
}
